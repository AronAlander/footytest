# Both logs are appended to by a Windows machine and a Linux Actions runner.
# Pin them to LF so the two never fight over line endings and produce
# whole-file diffs that hide the one row that actually changed.
predictions/log.csv text eol=lf
predictions/projection_log.csv text eol=lf
